import React from 'react'
const Error = () => {
    return (
        <div>
            Error
            {
                null.map((e, index) => {
                    return <div>Error</div>
                })
            }
        </div>
    )
}

export default Error